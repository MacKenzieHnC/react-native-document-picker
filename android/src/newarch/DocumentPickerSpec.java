package com.documentpicker;

import com.facebook.react.bridge.ReactApplicationContext;

abstract class DocumentPickerSpec extends NativeDocumentPickerSpec {
  DocumentPickerSpec(ReactApplicationContext context) {
    super(context);
  }
}
